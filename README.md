A collection of Platform sniffing functions.

## Usage
```javascript
var platfrom = require('env.platfrom');

if (platform.isIE) { /* do something */ }
if (platform.isIOS) { /* do something else */ }
...
```